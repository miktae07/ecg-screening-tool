# Copy file này thành .streamlit/secrets.toml (khi chạy local) và điền mật khẩu thật.
# KHÔNG commit file .streamlit/secrets.toml vào git (đã có trong .gitignore).
# Khi deploy lên Streamlit Community Cloud, dán nội dung tương tự vào
# App settings -> Secrets trên dashboard (không cần file này trên server).

app_password = "doi-mat-khau-nay"
